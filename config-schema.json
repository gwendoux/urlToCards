{
  "env": {
    "doc": "Application environment",
    "format": ["production", "development", "test"],
    "default": "production",
    "env": "NODE_ENV"
  },
  "log_level": {
    "doc": "Log level",
    "format": ["error", "warn", "info", "debug", "trace"],
    "default": "info"
  },
  "port": {
    "doc": "The port to bind",
    "format": "port",
    "default": 8005
  },
  "db_name": {
    "doc": "Name of database",
    "format": "*",
    "default": false
  },
  "db_model": {
    "doc": "Name of model use",
    "format": "*",
    "default": false
  }
}
